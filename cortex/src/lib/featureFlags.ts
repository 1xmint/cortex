// The Projects page has no working backend yet; flip this back on once it does.
// Gates both the sidebar link (GroupSidebar) and the /projects route (App) so
// the page cannot be reached by URL while it is hidden from navigation.
export const SHOW_PROJECTS_LINK: boolean = false;
