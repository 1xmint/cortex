# M-D-0013 progress

done: slices 1-2. Branch voice-delegation, pushed to cortex/agent/voice-delegation. Commit 9d72b3a3 (slice 1: live_session_config sends delegation.type=client + unit test). Commit c09cd165 (slice 2: voice_session.rs's run_billing_loop routes session.delegation.created to handle_delegation_created/run_voice_delegation, which calls chat_paid::send_paid_reply(..., voice_turn: true) — a new bool param threaded through all ~19 call sites, all existing ones pass false; agent_tools.rs gained tool_definitions_excluding_confirm() and is_confirm_risk(); chat_paid.rs's tool loop refuses a forced Confirm-risk call with "That needs confirmation in the chat; voice confirmation is not available yet." before the open_pr branch). Compiles clean under clippy --all-targets -D warnings. NOT yet committed/run: any tests for slice 2's new voice_session.rs code (no fake-sideband test exercises handle_delegation_created/run_voice_delegation/commentary_rx forwarding yet).
next: slice 3 — write the tests the packet lists (session config has client delegation done; still need: a delegation event runs the agent and sends commentary with the matching delegation id via the existing fake_live sideband harness around line 1329-1474 of voice_session.rs; Confirm tools absent from voice turns + forced call refused without executing, probably best as a chat_paid.rs unit test asserting tool_definitions_excluding_confirm() excludes open_pr/cancel_run and a send_paid_reply(voice_turn: true) run against a transport scripted to call open_pr comes back with the refusal tool_result and no pending_actions row; insufficient credits -> failure commentary + no charge; second delegation while busy -> refused; billing events still bill as before, i.e. run the existing run_billing_loop tests unchanged and confirm they still pass). Then slice 4 (cortex/plan/EXECUTION-STATE.md) and slice 5 (push, already done for code; still need `gh pr create -R 1xmint/cortex --draft --base main --head agent/voice-delegation`).
watch out for: confirmed doc facts (WebFetch of https://developers.openai.com/api/docs/guides/live-migration, not a raw quote — re-verify field names before trusting exactly) — session.delegation.created is {type, offset_ms, delegation:{id, type, target}} and carries NO request text; session.commentary.append is {type, event_id, delegation_id, content<=500 tokens}. Char cap used: 1200 (COMMENTARY_CHAR_CAP in voice_session.rs), reasoning documented inline (~4 chars/token, generous safety margin). run_voice_delegation always passes conversation_id: None because live voice sessions carry no conversation id anywhere in this codebase today (confirmed by grep) — flag this to Josh, not invented. cargo fmt -p cortex-api reformats unrelated files repo-wide: `git add` your own files BEFORE `git checkout -- crates/`, never after (lost slice-1 edits once this way already, had to redo). The commentary channel (commentary_tx/commentary_rx, capacity 8) is how the spawned delegation task talks back to run_billing_loop's single sideband owner — do not have the delegation task touch `sideband` directly.
